# Character-DeviceDriver
A simple character driver which allows the read operation to be executed from the kernel space to the userspace.
Basically,what is does is that-I have stored a few bytes and put it in kernel space.Now I have implemented the read operation in my driver and this driver will read the bytes of characters from the kernel space to the terminal which is the user space
